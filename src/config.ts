/**
 * Title: Dev network config
 * Purpose: Default LAN IP constants for local development and Deck connectivity examples.
 * Used for: Legacy dev scripts and examples referencing host/PC addresses.
 * Solves: Single place to update sample network endpoints during bring-up.
 * Does not: Persist or load user Ollama host — see usePluginSettings and persistOllamaIp.
 */
export const HostIp = '192.168.86.35';
export const PcIp = '192.168.86.52';
