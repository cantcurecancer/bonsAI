{
  "mcpServers": {
    "bonsai": {
      "command": "node",
      "args": [
        "packages/bonsai-mcp/dist/index.js"
      ],
      "env": {
        "BONSAI_REPO_ROOT": "${workspaceFolder}"
      }
    },
    "decky-plugin-studio": {
      "command": "node",
      "args": [
        "c:/Users/still/decky-plugin-studio/mcp-server/dist/index.js"
      ],
      "env": {
        "DECKY_STUDIO_WORKSPACE": "${workspaceFolder}",
        "DECKY_STUDIO_REPO": "c:/Users/still/decky-plugin-studio",
        "DEBUG_INGEST_PORT": "7684"
      },
      "_comment": "Point at the DPS source build, not an installed VSIX. A version-pinned extension path goes stale on every release: this was on 0.3.7 when the 0.3.8 killswitch upgrade replaced the folder, and every deck_* tool went missing with no error. DPS's own mcp.json carries the same warning."
    }
  }
}
